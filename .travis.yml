language: generic
sudo: required
services:
 - docker
 before_install:
  - docker build -t sunilkarigowda/docker-react -f Dockerfile.dev . 

  scripts:
   - docker run -e CI=true sunilkarigowda/docker-react npm run test